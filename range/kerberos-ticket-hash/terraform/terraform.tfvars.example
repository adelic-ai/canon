# Copy to terraform.tfvars (gitignored) and fill in. Do NOT commit the real file.
operator_ip    = "REPLACE/32"          # curl -s ifconfig.me  -> append /32
admin_password = "REPLACE-Str0ng-Pass" # 12+ chars, 3 of upper/lower/digit/symbol
# location    = "eastus"               # optional overrides
# vm_size     = "Standard_B2s"
# image_sku   = "2025-datacenter-g2"
