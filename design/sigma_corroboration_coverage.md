# Sigma-corroboration coverage map

How much does the *independent* deduped Sigma panel actually corroborate each registered
detector? Measured on real representative events (OTRF Sysmon + flaws CloudTrail). The
negatives are recorded data, not silence — `no-overlap`/`not-evaluable`/`no-same-logsource`
each name a distinct, honest reason the external panel does not confirm.

Generated by `packages/detection/experiments/sigma_corroboration_coverage.py`.

## T1003.001 — CORROBORATED
- logsource: `process_access`
- panel: 79 tagged → 15 same-logsource → 7 deduped classes → 6 evaluable → 1 fired
- fired: proc_access_win_lsass_dump_comsvcs_dll.yml
- note: the comsvcs LSASS dump — corroborated by the dedicated CallTrace rule

## T1580 — CORROBORATED
- logsource: `{'product': 'aws', 'service': 'cloudtrail'}`
- panel: 1 tagged → 1 same-logsource → 1 deduped classes → 1 evaluable → 1 fired
- fired: aws_enum_buckets.yml
- note: S3 enumeration — aws_enum_buckets fires on the native ListBuckets event

## T1098 — no-overlap
- logsource: `{'product': 'aws', 'service': 'cloudtrail'}`
- panel: 45 tagged → 4 same-logsource → 2 deduped classes → 1 evaluable → 0 fired
- fired: —
- note: SigmaHQ's account-manip rules cover other IAM ops (UpdateLoginProfile, Route53) than our CreateUser/AccessKey family — evaluated, no overlap

## T1496 — no-same-logsource
- logsource: `{'product': 'aws', 'service': 'cloudtrail'}`
- panel: 13 tagged → 0 same-logsource → 0 deduped classes → 0 evaluable → 0 fired
- fired: —
- note: region-sweep — SigmaHQ's T1496 rules are azure/zeek, none aws/cloudtrail

## T1558.003 — no-overlap
- logsource: `{'product': 'windows', 'service': 'security'}`
- panel: 17 tagged → 5 same-logsource → 5 deduped classes → 4 evaluable → 0 fired
- fired: —
- note: the evaluable security rules target tooling artifacts (Rubeus EID 4611), not the behavioral 4769 RC4 fan-out our detector flags — a detection-model gap, not just naming

## T1550.003 — no-same-logsource
- logsource: `{'product': 'windows', 'service': 'security'}`
- panel: 4 tagged → 0 same-logsource → 0 deduped classes → 0 evaluable → 0 fired
- fired: —
- note: pass-the-ticket — no evaluable windows/security rule (SigmaHQ's are process_creation)

## T1110.003 — no-same-logsource
- logsource: `{'product': 'windows', 'service': 'security'}`
- panel: 0 tagged → 0 same-logsource → 0 deduped classes → 0 evaluable → 0 fired
- fired: —
- note: password spraying — no SigmaHQ rule tagged attack.t1110.003 at all
